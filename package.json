{
  "name": "swamp-sync-v2",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun --env-file=.env run --filter '*' dev",
    "dev:server": "bun --env-file=.env run --filter @swamp-sync-v2/server dev",
    "dev:web": "bun --env-file=.env run --filter @swamp-sync-v2/web dev",
    "build": "bun --env-file=.env run --filter '*' build",
    "test": "bun --env-file=.env run --filter @swamp-sync-v2/server test",
    "db:generate": "bun --env-file=.env run --filter @swamp-sync-v2/server db:generate",
    "db:migrate": "bun --env-file=.env run --filter @swamp-sync-v2/server db:migrate",
    "db:studio": "bun --env-file=.env run --filter @swamp-sync-v2/server db:studio",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  }
}
