{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun run --filter=@swamp-sync-v2/server build",
    "watchPatterns": ["/packages/server/**", "/package.json", "/bun.lock"]
  },
  "deploy": {
    "preDeployCommand": ["bun --filter=@swamp-sync-v2/server run db:migrate"],
    "startCommand": "bun run --filter=@swamp-sync-v2/server start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "sleepApplication": true,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
